name: Яндекс.Музыка Бот

on:
  workflow_dispatch:  # Ручной запуск
  push:
    branches: [ main ]
  schedule:
    - cron: '0 */6 * * *'  # Каждые 6 часов

jobs:
  run-bot:
    runs-on: windows-latest  # Только Windows, т.к. используем .bat
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      
    - name: ⎔ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'  # или версия, которую используете
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci  # или npm install
      shell: cmd
      
    - name: 🚀 Run start.bat
      run: start.bat
      shell: cmd
      env:
        CI: true  # Отключаем интерактивные режимы если есть
